# Cloud-only dependencies for Vercel deployment (no heavy PaddleOCR/PaddlePaddle)
# These are used when GROQ_API_KEY is present (Vercel cloud environment)

# LLM Integration & Schema Validation
groq>=0.9.0
pydantic>=2.0.0

# Web Backend API Framework
fastapi>=0.100.0
uvicorn>=0.20.0
python-multipart>=0.0.6

# Image Processing (lightweight, for base64 conversion)
Pillow>=9.0.0
pypdfium2>=4.0.0
